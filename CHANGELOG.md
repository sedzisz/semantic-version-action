# Changelog

## v2
- Rewrote entrypoint from Bash to Python 3
- Removed `jq`, `bash`, `curl` from Docker image
- Fixed `map_to_bump` returning multiple lines causing version bump failure
- Smaller Docker image

## v1
- Initial release
